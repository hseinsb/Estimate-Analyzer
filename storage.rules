rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Estimates storage - authenticated users can upload and read
    match /estimates/{year}/{month}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
